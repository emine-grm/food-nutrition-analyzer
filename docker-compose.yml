version: '3.8'

services:
  meal-coach:
    build: .
    ports:
      - "8000:8000"
    environment:
      - USDA_API_KEY=${USDA_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped